# Benchmark Evidence

Evidence directories contain immutable JSON outputs for published fixed-corpus
results. Each file preserves benchmark metadata, exact runtime configuration,
per-run transcripts and scores, aggregate summaries, and skips/errors.

Generated working outputs remain under ignored `output/`; publish a reviewed
copy here only when updating `RESULTS.md`.
